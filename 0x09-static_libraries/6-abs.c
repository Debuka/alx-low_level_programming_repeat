#include"main.h"
/**
 * _abs - returns the absolute value of a given integer
 * @n: the inter that will be checked
 * Return: the absolutr value determined.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
