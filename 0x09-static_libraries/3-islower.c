#include"main.h"
/**
 * _islower - detects lower case characters
 * @c: the character that will be checked
 * Return: 1 if lowercase, 0 if not.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
